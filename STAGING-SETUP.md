# بناء بيئة الاستينج — دليل التشغيل

>  نسخة تانية كاملة من ميزان، بقاعدة بيانات **منفصلة تمامًا**،
> عشان تجرّب عليها التعديلات قبل ما توصل لعملائك.
>
> أربع مراحل. **كل مرحلة ليها فحص في آخرها — متعديش للي بعدها
> قبل ما الفحص ينجح.** المرحلة اللي بتعدّيها وهي نص شغّالة
> بتظهر بعدين في أسوأ وقت.

---

## المبدأ اللي الدليل كله مبني عليه

**الجدار بين الإنتاج والتجربة هو قاعدة البيانات، مش الرابط.**

موقع تاني بيكلّم نفس القاعدة مش استينج — ده باب تاني لنفس البيت.
لو جرّبت عليه تعديل غلط، البيانات اللي اتبوّظت هي بيانات عملائك.

تشبيه: مش كفاية تحطّ لافتة "صالة تدريب" على أوضة. لو البساط
اللي بتتمرن عليه هو نفس بساط النزال، أي إصابة حقيقية.

---

# ═══ مرحلة ٠ — انزع اللغم (خمس دقايق) ═══

## المشكلة

ملف `supabase/00_reset.sql` عندك قفل الأمان بتاعه **مفتوح**:

```sql
v_confirm text := 'YES';   -- ←←← غيّرها لـ 'YES' عشان يشتغل
```

التعليق بيقول "غيّرها لـ YES" وهي أصلاً YES. يعني الملف ده لو
اتلصق في محرر الإنتاج بالغلط، بيمسح كل الجداول على طول من غير
ما يسأل.

وده أخطر دلوقتي عن أي وقت فات: الخطة المجانية في سوبابيز مالهاش
نسخ احتياطية للتحميل ولا استرجاع لنقطة زمنية. نظامك كله موجود
في مكان واحد بس.

## العمل

من جيت هب، افتح `supabase/00_reset.sql` وغيّر السطر ده لـ:

```sql
  v_confirm text := 'NO';    -- ←←← غيّرها لـ 'YES' عشان يشتغل
```

**⚠ ولا تشغّل الملف ده خالص في الدليل ده.** مشروع سوبابيز
الجديد بيبقى فاضي أصلاً، فمفيش حاجة تتصفّر.

### ✅ الفحص
افتح الملف على جيت هب وشوف بعينك إن مكتوب `'NO'`.

---

# ═══ مرحلة ١ — قاعدة بيانات الاستينج ═══

الخطة المجانية بتسمح بمشروعين نشطين. عندك واحد، فده التاني.

> ⚠ مشروع الاستينج هيتوقّف مؤقتًا لو عدّى أسبوع من غير استخدام.
> ده طبيعي — بترجّعه من اللوحة بضغطة. مش عطل.

## ١.١ اعمل المشروع

`supabase.com` ← **New project** ← سمّيه `meezan-staging`.
اختار نفس المنطقة الجغرافية بتاعة مشروع الإنتاج.

⚠ احفظ كلمة مرور قاعدة البيانات في مكان تاني — سوبابيز مش
بتوريها تاني.

## ١.٢ فعّل pg_cron

`Database` ← `Extensions` ← دوّر على **pg_cron** ← فعّلها.

من غيرها ملف `03` و`27` هيطلّعوا رسالة إنها مش موجودة ويكمّلوا —
بس الفحص الأمني اليومي مش هيتجدول.

## ١.٣ شغّل ملفات SQL بالترتيب ده بالظبط

`SQL Editor` ← `New query` ← الصق ← **Run**. واحد ورا التاني.

> **⚠ التلات خطوات المعلّمة 🔴 لازم تتشغّل لوحدها في نافذة
> query فاضية.**
>
> السبب: بوستجرس بيسجّل قيمة enum الجديدة، لكن ما بيخليكش
> تستخدمها في **نفس المعاملة**. لازم تقفل الدفتر وتفتحه تاني.
> الفخ ده عدّى على المشروع أربع مرات — متكرروش.

| # | الملف | ملاحظة |
|---|---|---|
| — | `00_reset.sql` | **⛔ متشغّلوش. تخطّاه.** |
| 1 | `01_schema.sql` | الجداول والأدوار |
| 2 | `02_functions.sql` | دوال الدخول والصلاحيات |
| 3 | `03_cron.sql` | التنظيف الدوري |
| 4 | `04_phase0.sql` | حذف ناعم + خزائن |
| 5 | `05_treasury.sql` | ✅ آمن هنا — القاعدة فاضية |
| 🔴 | **السطر تحت لوحده** | قيمة enum جديدة |
| 6 | `06_sales.sql` | المنتجات والبيع |
| 7 | `07_expense_reasons.sql` | |
| 8 | `08_devices_customers.sql` | |
| 9 | `09_customer_link.sql` | |
| 🔴 | `10_platform_role.sql` | **لوحده** — enum |
| 11 | `11_tenants.sql` | طبقة المحلات |
| 12 | `12_platform.sql` | |
| 13 | `13_provisioning.sql` | |
| 14 | `14_tenant_purge.sql` | |
| 15 | `15_uniqueness_fix.sql` | |
| 🔴 | `16_refund_enum.sql` | **لوحده** — enum |
| 17 | `17_returns.sql` | |
| 18 | `18_staff_refund.sql` | |
| 19 | `19_income.sql` | |
| 20 | `20_alerts.sql` | |
| 21 | `21_transfers.sql` | |
| 22 | `22_suppliers.sql` | |
| 23 | `23_device_specs.sql` | |
| 24 | `24_maintenance.sql` | |
| 25 | `25_unlock_open.sql` | |
| 26 | `26_maintenance_search.sql` | |
| 27 | `27_security_audit.sql` | الفحص الأمني |
| 28 | `28_warranty_closing.sql` | الضمان واليومية |
| 29 | `29_email_uniqueness_audit_fix.sql` | |
| 30 | `30_payment_methods.sql` | وسائل الدفع |
| 31 | `31_treasury_transfer.sql` | |
| 32 | `32_platform_announcements.sql` | |
| 33 | `33_cash_naming.sql` | |
| 34 | `34_price_trigger_lock.sql` | قفل دالة المشغّل |
| — | `35_expense_reasons_dedupe.sql` | **⛔ أداة، مش migration. تخطّاه.** |
| — | `36_diagnostics.sql` | **قراءة بس** — للفحص تحت |

### 🔴 الخطوة المعلّمة بين ٥ و ٦

في نافذة query **فاضية**، الصق السطر ده لوحده واضغط Run:

```sql
alter type movement_type add value if not exists 'SALE';
```

بعدين افتح نافذة جديدة وكمّل بملف `06_sales.sql`.

> ليه؟ نوع الحركة `movement_type` بيتعمل في ملف `05`، وقيمة
> `SALE` بتتضاف بعده. ودالة البيع في `06` بتستخدمها. لو
> التلاتة في معاملة واحدة، بوستجرس بيرفض.
>
> ملف `06` نفسه فيه حارس بيقولك بالنص لو نسيت الخطوة دي.

## ١.٤ ⚠ لو ملف وقع

**قف. متكملش.** كل ملف في آخره فحوصات بالمتوقّع مكتوب جنبها —
اقرا الرسالة وابعتهالي. الملف اللي بيقع وبتعدّيه بيسيب النظام
ناقص حاجة ما بتظهرش غير عند أول استخدام حقيقي.

### ✅ الفحص — دي أهم لحظة في المرحلة دي

شغّل في محرر SQL:

```sql
select check_name as "الفحص", status as "الحالة", detail as "التفصيل"
from fn_security_audit()
order by (status = 'OK'), check_name;
```

**المتوقّع: تمن صفوف، كلهم `OK`.**

أي `FAIL` معناه إن السلسلة ما أعادتش بناء النظام صح — والعمود
التالت بيقول إيه بالظبط.

> 💡 ولاحظ إن المرحلة دي هي كمان **بروفة استرجاع**. لو التمن
> فحوصات طلعوا أخضر، يبقى ملفات SQL بتاعتك تقدر تبني النظام من
> الصفر فعلاً — وده معناه إن عندك نسخة تانية بحق، مش مجرد ثقة.
>
> ولو طلعوا أحمر، تبقى عرفت النهاردة بدل ما تعرف يوم الكارثة.

## ١.٥ خُد المفاتيح

`Settings` ← `API` ← انسخ:

- **Project URL** ← ده `SUPABASE_URL` بتاع الاستينج
- **service_role** ← ده `SUPABASE_SERVICE_KEY` بتاع الاستينج

⚠ اتأكد إنه `service_role` مش `anon`. الاتنين شكلهم واحد
(JWT طويل) والفرق جوّه. فيه فحص عند الإقلاع بيرفض `anon`
برسالة واضحة، بس أحسن ما تدوّر.

---

# ═══ مرحلة ٢ — فصل رابط القاعدة (⚠ الترتيب حسّاس) ═══

## المشكلة

`SUPABASE_URL` مكتوب جوّه `[vars]` في `wrangler.toml`. والملف ده
**بيلغي أي متغيّر Text من لوحة كلاودفلير**.

يعني لو عملت نشرة استينج دلوقتي، هتفضل بتكلّم قاعدة **الإنتاج**،
وإنت فاكر إنها منفصلة. وهي هتشتغل تمام وتبان صح — وده أسوأ نوع
عطل ممكن.

الأسرار (Secret) بس هي اللي بتفضل شغّالة من اللوحة. فبننقل
الرابط لهناك.

## 🔴 الترتيب — اتبعه حرفيًا

**الخطوة ١ الأول، وبعدين ٢. لو عكست، الموقع الحيّ هيقع.**

### ١) حطّ السر في الإنتاج

Cloudflare ← مشروعك ← `Settings` ← `Variables and Secrets`
← تبويب **Production** ← Add:

| الاسم | النوع | القيمة |
|---|---|---|
| `SUPABASE_URL` | **Secret** | `https://fjhldlitpzekrskrbydu.supabase.co` |

⚠ **Secret** مش **Text**. لو حطّيته Text، `wrangler.toml`
هيدهس عليه ونرجع لنقطة الصفر.

### ٢) وبعدين ارفع `wrangler.toml` الجديد

الملف اللي بعتهولك، على المسار `wrangler.toml` في جذر المشروع.

### ✅ الفحص
استنى النشر يخلص، وافتح `meazan.pages.dev` وسجّل دخول عادي.

**لو الموقع فتح وسجّلت دخول، الرابط بقى بيتقرا من الأسرار.**
لو طلعت رسالة "متغيّرات ناقصة"، يبقى الخطوة ١ ما اتعملتش
صح — ارجعلها.

---

# ═══ مرحلة ٣ — موقع الاستينج ═══

## ٣.١ اعمل فرع `staging` في جيت هب

من تطبيق جيت هب: قايمة الفروع ← **New branch** ← الاسم
`staging` ← مأخوذ من `main`.

كلاودفلير Pages بتعمل نشرة معاينة لكل فرع تلقائيًا، وبتديها
رابط ثابت شكله كده تقريبًا:

```
staging.meazan.pages.dev
```

الرابط الدقيق بيظهر في كلاودفلير ← `Deployments` جنب النشرة.

> ⚠ الرابط ده **عام** — أي حد يعرفه يقدر يفتحه. `robots.txt`
> بيمنع فهرسته في محركات البحث، بس مش بيقفله. متحطّش فيه بيانات
> حقيقية أبدًا.

## ٣.٢ ولّد أسرار جديدة للاستينج

في محرر SQL بتاع **الاستينج**، شغّل السطر ده **تلات مرات**
وخُد ناتج مختلف كل مرة:

```sql
select encode(gen_random_bytes(48), 'base64') as secret;
```

التلاتة دول لـ `JWT_SECRET` و`REFRESH_TOKEN_PEPPER`
و`SETUP_SECRET`.

> ⚠ ليه مفاتيح مختلفة عن الإنتاج؟ عشان بطاقة دخول اتعملت على
> الاستينج ما تبقاش صالحة على الإنتاج. الفصل في القاعدة بيمنع
> ده أصلاً، بس القفلين أحسن من واحد — ومفيش تكلفة.

## ٣.٣ حطّ أسرار المعاينة

Cloudflare ← `Settings` ← `Variables and Secrets`
← تبويب **Preview** ← Add. **كلهم Secret**:

| الاسم | القيمة |
|---|---|
| `SUPABASE_URL` | رابط مشروع **الاستينج** |
| `SUPABASE_SERVICE_KEY` | مفتاح `service_role` بتاع **الاستينج** |
| `JWT_SECRET` | الناتج الأول |
| `REFRESH_TOKEN_PEPPER` | الناتج التاني |
| `SETUP_SECRET` | الناتج التالت |

⚠ **راجع `SUPABASE_URL` مرتين.** ده الجدار كله. لو اتلصق رابط
الإنتاج بالغلط، الاستينج بيبقى باب تاني لنفس البيت — وهيشتغل
تمام ومش هيقولك.

## ٣.٤ انشر

اعمل أي تعديل بسيط على فرع `staging` (زوّد سطر فاضي في
`README.md` مثلًا) عشان النشر يتشغّل.

### ✅ الفحص — التلاتة دول مع بعض

| # | الاختبار | المتوقّع |
|---|---|---|
| 1 | افتح `staging.meazan.pages.dev/login` | صفحة الدخول بتفتح |
| 2 | جرّب تدخل بحسابك بتاع الإنتاج | **يترفض** — الحساب مش موجود في قاعدة الاستينج |
| 3 | افتح `staging.meazan.pages.dev/setup` | 404 |

> 🔴 **الاختبار رقم ٢ هو الفحص الحقيقي.** لو دخلت بحساب
> الإنتاج بنجاح، يبقى الاستينج بيكلّم قاعدة الإنتاج — **قف
> فورًا** وراجع `SUPABASE_URL` في تبويب Preview.

---

# ═══ مرحلة ٤ — التأسيس ومحلات التجربة ═══

## ٤.١ اعمل مشغّل المنصّة

افتح `staging.meazan.pages.dev/platform-setup` واملأ:

- سرّ الإعداد ← `SETUP_SECRET` اللي حطّيته
- اسم المستخدم، الاسم الكامل
- كلمة مرور (١٢ حرف على الأقل)
- مفتاح ثاني (١٦ حرف على الأقل، **مختلف** عن كلمة المرور)

## ٤.٢ ⚠ امسح `SETUP_SECRET` فورًا

Cloudflare ← تبويب **Preview** ← امسح `SETUP_SECRET`.

الباب ده بيتفتح مرة واحدة. سيبانه مفتوح معناه إن أي حد يعرف
السرّ يقدر يعمل مشغّل منصّة.

## ٤.٣ افتح محلات التجربة

ادخل على `/platform` بحساب مشغّل المنصّة، واعمل محلّين:

| المحل | الكود | الفروع | الغرض |
|---|---|---|---|
| محل التجربة الأول | `TEST1` | فرعين | كل الاختبارات العادية |
| محل التجربة التاني | `TEST2` | فرع واحد | اختبار عزل المحلات |

وفي `TEST1` اعمل الحسابات دي (الأسماء دي مذكورة في ملف
`TESTSPRITE-PRD.md`):

| الحساب | الدور | الفرع |
|---|---|---|
| `owner` | صاحب المحل | — |
| `manager` | مدير فرع | الفرع الأول |
| `staff` | مندوب مبيعات | الفرع الأول |
| `manager_b` | مدير فرع | الفرع التاني |

### ✅ الفحص النهائي

| # | الاختبار | المتوقّع |
|---|---|---|
| 1 | ادخل بـ `owner` على `TEST1` | يفتح |
| 2 | ادخل بـ `manager` وافتح `/report` | مفيش تكلفة ولا هامش |
| 3 | ادخل بـ `manager` وجرّب تشوف بيانات الفرع التاني | يترفض |
| 4 | شغّل `fn_security_audit()` تاني | التمنية `OK` |
| 5 | ادخل على الإنتاج وتأكد إن محلات التجربة **مش موجودة** فيه | مش موجودة |

**الاختبار رقم ٥ هو اللي بيقفل الدايرة.** لو محلات التجربة
ظهرت في الإنتاج، الجدار مكسور.

---

# ═══ بعد كده ═══

**طريقة شغلك الجديدة:**

```
تعديل  →  فرع staging  →  جرّب على staging.meazan.pages.dev
                       →  عدّى؟  →  ادمج على main
```

وتست سبرايت بتتوجّه لـ **`staging.meazan.pages.dev`** مش
للإنتاج. وساعتها تقدر تسيبها تجرّب أي حاجة — حتى الحاجات
المدمّرة — من غير ما ترمش.

---

## ⚠ حاجات تفضل مختلفة عن الإنتاج

| البند | ملاحظة |
|---|---|
| مشروع الاستينج بيتوقّف بعد أسبوع خمول | ترجّعه من لوحة سوبابيز بضغطة |
| رابط الاستينج عام | متحطّش فيه بيانات حقيقية |
| مفيش نسخ احتياطية على الخطة المجانية | ولا في الإنتاج ولا الاستينج |
| بيانات الاستينج مش نسخة من الإنتاج | بيانات تجربة اتعملت بإيدك. لو محتاج بيانات واقعية، اعملها بإيدك — **متنقلش بيانات عملاء حقيقيين للاستينج أبدًا** |
